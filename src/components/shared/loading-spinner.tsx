'use client'

// Re-export from unified HouseLoader for backward compatibility
export { 
  HouseLoader as UrugostayLoader, 
  HouseLoader as LoadingSpinner, 
  PageHouseLoading as PageLoadingSpinner 
} from './HouseLoader'
